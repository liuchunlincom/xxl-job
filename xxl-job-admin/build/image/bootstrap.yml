spring:
  profiles:
    active: prod
  application:
    name: xxl-job-admin
  cloud:
    nacos:
      config:
        username: ${NACOS_AUTH_USERNAME}
        password: ${NACOS_AUTH_PASSWORD}
        server-addr: ${NACOS_HOST}
        file-extension: yaml
        namespace: ${NACOS_NAMESPACE}
        group: ${NACOS_GROUP}
